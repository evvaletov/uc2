{"author":{"id":"40e3ba2788b3b9fc163c354afc44e5f9d225bcf0a27e679aaa878dd6135c6dcb"},"ops":[{"type":1,"timestamp":1781352853,"nonce":"iqdNa1ZckGP/st3yvrapAlJMTt4=","title":"SECURITY: decoder memory-safety on malformed input (ht_dec stack overflow, assemble_name NULL-deref, delta/LZ OOB)","message":"task-qa 2026-06-13 (codex + grok + adversarial Workflow harness, cross-verified) found multiple memory-safety defects in lib/src/decompress.c reachable from a crafted archive:\n1. BLOCKING ht_dec() Huffman RepeatCode expansion (line ~1355): 'for (; n\u003e0; n--) *symp++ = val;' has no symp\u003csyme bound -\u003e stack buffer overflow past the local stream[NumSymbols] array (write OOB, up to ~18 bytes). Fix: add '\u0026\u0026 symp \u003c syme' to the loop. Adversarial verifier confirmed high-confidence with a concrete trace.\n2. BLOCKING assemble_name() (line ~502): put_utf8 can return NULL when the 300-byte name buffer fills; the following assert(d) is a no-op under NDEBUG, so the next iteration dereferences NULL. Fix: 'if (!d) break;' as copy_long_name already does.\n3. BLOCKING struct delta index OOB (lines 954/968/980): val[8] but delta methods 38/39/48/49 set d.size=9/10, so d.index reaches 8/9 -\u003e OOB read+write of d.val[8]/[9]. Fix: reject delta\u003e8 in the method dispatch (or size val[] to the real max).\n4. BLOCKING/INFO-LEAK LZ copy distance unvalidated in BOTH decompressor_rans (~1150) and decompressor_ultra/decompress_block (~1563): dist is not bounded against produced history, so (u16)(tail-dist) can read uninitialized window/master bytes into the output (the f049d6d fix added the LENGTH guard but not a DISTANCE/history guard). Fix: cap valid dist to master_size + produced and reject larger as UC2_Damaged. Complements 69e8e52.","files":null}]}